import rss from '@astrojs/rss'; import {getCollection} from 'astro:content';
export async function GET(context:any){const posts=(await getCollection('blog')).sort((a,b)=>b.data.pubDate.valueOf()-a.data.pubDate.valueOf());return rss({title:'Image Resizer Guides',description:'Practical articles about image resizing, compression, formats, and private browser processing.',site:context.site??'https://image-resizer.github.io',items:posts.map(post=>({title:post.data.title,description:post.data.description,pubDate:post.data.pubDate,link:`/blog/${post.id.replace(/\.(md|mdx)$/,'')}/`}))})}
